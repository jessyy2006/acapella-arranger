# Aca-Adapt environment variables.
# Copy to `.env` (git-ignored) and fill in as needed.

# Optional: HuggingFace token for faster/authenticated jaCappella download
# HF_TOKEN=

# Optional: Streamlit app log level (DEBUG | INFO | WARNING | ERROR)
LOG_LEVEL=INFO
